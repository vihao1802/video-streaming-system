export interface PresignedUrls {
  video_url: string;
  video_id: string;
  thumbnail_url: string;
  thumbnail_id: string;
}

export interface UploadMetadata {
  video_id: string;
  title: string;
  description: string;
}
